import { styled } from '@mui/material/styles';
import Typography from '@mui/material/Typography';

const ToolbarFieldTitle = styled(Typography)(({ theme }) => ({
  marginRight: theme.spacing(2),
}));

export default ToolbarFieldTitle;
