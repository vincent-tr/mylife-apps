'use strict';

import { React, PropTypes, mui } from 'mylife-tools-ui';
import DocumentThumbnailList from './document-thumbnail-list';

const DocumentList = ({ ...props }) => (
  <DocumentThumbnailList {...props} />
);

export default DocumentList;
